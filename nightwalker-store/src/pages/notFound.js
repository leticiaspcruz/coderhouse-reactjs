import React from "react";
import { NavBar, ErrorMessage } from '../components';

const NotFound = () => {
  return (
    <>
     <NavBar />
     <ErrorMessage />
  </>
  );
};

export default NotFound;